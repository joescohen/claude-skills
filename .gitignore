# Only track explicitly added skills — ignore everything else by default
*
!.gitignore
!system-validation/
!system-validation/**
!skill-auditor/
!skill-auditor/**
!chat-triage/
!chat-triage/**
!rubric-anchored-recursion/
!rubric-anchored-recursion/**
